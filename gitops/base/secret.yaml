# Placeholder ONLY. Real value is synced from Azure Key Vault on the node:
#   az keyvault secret show ... (via VM managed identity, no creds in code)
#   kubectl create secret generic app-secret -n <ns> --from-literal=APP_SECRET=<value>
# Committed value is a dummy and is NEVER the production secret.
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: app
type: Opaque
stringData:
  APP_SECRET: "PLACEHOLDER_REPLACED_FROM_KEYVAULT"
